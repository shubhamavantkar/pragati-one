import 'package:flutter/material.dart';

class ExpandableWorkOrder extends StatelessWidget {
  const ExpandableWorkOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
